# digital-land-efs-sync

## Dependencies

- python
- sqlite
- dbhash - this is set up locally, the .env file adds the path to look for relative to the repo path to your PATH variable. use `make dbhash` and soruce the .env file to get working  